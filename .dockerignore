# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# audio storage
/audio

# drizzle
/drizzle

# git
# NOTE: `.git/` is intentionally NOT ignored. The `fumadocs-mdx`
# `lastModified` plugin (see source.config.ts) shells out to `git log`
# to stamp each MDX page with its last-commit time. Without history
# present in the builder stage the plugin errors and `next build`
# fails. `.git` only lands in the `builder` stage; the `runner` stage
# does not copy it, so the published image is unaffected.
.gitignore

# IDE
.vscode
.idea

# README
README.md

# The sidecar has its own image, but Next type-checks the test that imports its
# pure logic module during the app build. Keep only that module in this context;
# the app image's runner stage still copies neither it nor the tests.
/agent-bridge/*
!/agent-bridge/lib.mjs
